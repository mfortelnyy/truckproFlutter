name: truckpro
description: "Truck Safety"
version: 0.1.0

environment:
  sdk: '>=3.4.3 <4.0.0'

dependencies:
  mime: ^1.0.0
  intl: ^0.17.0
  stop_watch_timer: ^3.2.1
  flutter:
    sdk: flutter
  http: ^1.0.0
  jwt_decoder: ^2.0.1
  image_picker: ^1.1.2
  file_picker: ^8.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/registration_bg.png
